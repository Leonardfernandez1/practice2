name: Flujo uno
on: workflow_dispatch
jobs:
  dependencias_job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Obtener código
        uses: actions/checkout@v4

      - name: Instalar NodeJS
        uses: actions/setup-node@v4
        with: 
          node-version: 20 

      - name: Instalar Dependencias
        run: npm ci

      - name: lint
        run: npm run lint

      - name: test
        run: npm test

      - name: build
        run: npm run build

  desplegarjob:
    needs: dependencias_job
    runs-on: ubuntu-latest
    steps:
      - name: Despliegue
        run: echo "Se ha realizado el despliegue"
